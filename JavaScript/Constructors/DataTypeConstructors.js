String()
Number()
Boolean()
Object()
Array()
Function()

// Example usage:
const str = String(123); // "123"
const num = Number("456");  // 456
const bool = Boolean(1);    // true     
const obj = Object({ a: 1 }); // { a: 1 }
const arr = Array(1, 2, 3); // [1, 2, 3]
const func = Function("a", "b", "return a + b;"); // function that adds two numbers
